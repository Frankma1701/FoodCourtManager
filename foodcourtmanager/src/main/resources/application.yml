server:
  port: 8091

spring:
  datasource:
    url: jdbc:mysql://localhost/food_court_manager?serverTimezone=UTC
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

springdoc:
  swagger-ui:
    enabled: true
    path: swagger-ui.html